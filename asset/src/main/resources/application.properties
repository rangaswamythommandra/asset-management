spring.application.name=asset

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/assetdb?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Usharani@999
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Initialize data (enabled to create tables and insert sample data)
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=embedded

# JWT Secret
jwt.secret=your_secret_key_here_change_this
